import { Container } from '@mantine/core';

export default function FraudListHome() {
  return (
    <Container>
      <ul>
        <li>show recent past picks if week lull</li>
        <li>if week in session, show picks</li>
      </ul>
    </Container>
  );
}
